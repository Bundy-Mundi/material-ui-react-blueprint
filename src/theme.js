import { red, green, blue, purple } from '@material-ui/core/colors';

const theme = {
    pallete:{
        red,
        blue,
        green,
        purple
    },
    spacing:{
        sm: "5rem",
        md: "0.8rem",
        lg: "1.1rem",
        xl: "1.4rem"
    }
};

export default theme;
